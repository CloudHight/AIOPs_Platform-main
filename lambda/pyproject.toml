[project]
name = "aiops-lambda"
version = "0.1.0"
description = "AIOps Lambda control plane package"
requires-python = ">=3.12"

dependencies = [
  "boto3>=1.34",
  "botocore>=1.34",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
